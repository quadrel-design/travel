import 'package:flutter/material.dart';

/// Defines shared layout constants for consistent UI elements like Grids.

// Padding around entire GridViews or similar list layouts
const EdgeInsets kGridPadding = EdgeInsets.zero;

// Default number of columns for standard image/card grids
const int kGridCrossAxisCount = 3;

// Default spacing between columns in a grid
const double kGridCrossAxisSpacing = 8.0;

// Default spacing between rows in a grid
const double kGridMainAxisSpacing = 8.0;
