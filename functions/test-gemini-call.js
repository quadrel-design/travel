const dotenv = require("dotenv"); const {GoogleGenerativeAI} = require("@google/generative-ai"); dotenv.config(); const apiKey = process.env.GEMINI_API_KEY; console.log("GEMINI_API_KEY exists:", !!apiKey); const genAI = new GoogleGenerativeAI(apiKey); async function testGemini() { try { const model = genAI.getGenerativeModel({model: "gemini-2.0-flash-lite"}); const result = await model.generateContent("Say hello"); console.log("Gemini response:", result.response.text()); console.log("API key is working correctly!"); } catch (error) { console.error("Error testing Gemini API:", error.message); } } testGemini();
